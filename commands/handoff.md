---
name: handoff
description: Compact this conversation into a portable handoff document for another agent
---

Follow `skills/handoff/SKILL.md`. Write the document to the OS temp directory (not the workspace). Include a suggested-skills section. Reference existing artifacts by path or URL instead of copying them. Redact secrets. Treat invocation arguments as the next session's focus. Distinct from `/capture-a-skill`.
