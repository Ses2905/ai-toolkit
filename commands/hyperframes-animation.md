---
name: hyperframes-animation
description: Compose seek-safe HyperFrames motion from atomic GSAP rules and adapters
---

Follow `skills/hyperframes-animation/SKILL.md`. Read `skills/hyperframes-core/SKILL.md` for the composition contract. Default: pick 2-4 rules from `rules-index.md` on one paused GSAP timeline. Load a blueprint only when the scene matches. Distinct from `/react-bits`. Do not run `npx hyperframes` unless asked.
