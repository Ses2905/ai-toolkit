---
name: ppt-master
description: Generate or edit a native PPTX deck
---

Follow `skills/ppt-master/SKILL.md`. Run the attribution guard, then load only the selected route. Distinct from `/codex-ppt` (full-slide images) and `/html-ppt` (HTML). Audio binaries and AI image-comparison PNGs are not vendored; copy from upstream if needed.
