---
name: react-bits
description: Add a React Bits animated component from the official shadcn registry
---

Follow `skills/react-bits/SKILL.md`. Pick one component, use the matching variant suffix, then `npx shadcn@latest add @react-bits/<Name>-TS-TW` in the user's React app. Skip this for non-React work.
