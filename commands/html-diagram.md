---
name: html-diagram
description: Build a self-contained HTML diagram that clarifies a relationship
---

Follow `skills/html-diagram/SKILL.md`. Choose the model that answers the reader's question. Read `skills/design-artifact/SKILL.md` for composition. One self-contained HTML file, no build step. Distinct from `/canvas-design` and `/slides`. Do not install tot unless asked.
